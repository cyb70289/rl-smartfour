Implement smart-four alphazero model
- game rules in `game.md`
- model draft design in `model.md`
- ui is already implemented per `ui.md`, code in subdir "ui"

Let's focus only on the model, forget about the UI.
Implement the model training and inference.

NOTES:
- `model.md` is a very basic draft, just take it as reference, propose your own design if anything not suitable
- grill me relentlessly until we are fully aligned before coding
- use TDD to drive the coding, make sure tricky or error prone features are well tested
- use pytorch, put model related code in "model" dir, install python packages in .venv dir
