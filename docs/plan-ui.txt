Implement smart-four game UI. Ignore model for now.
- game rules in `game.md`
- ui requirement in `ui.md`
- model details in `model.md`, it's currently TBD

Let's focus only on the UI, forget about the model.
Implement the full featured UI. For machine player, serve a dummy model which plays randomly. It will be updated once real model is trained. A well defined interface will be good.

NOTES:
- grill me relentlessly until we are fully aligned before coding
- use TDD to drive the coding, make sure tricky or error prone features are well tested
