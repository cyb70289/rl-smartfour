# Smart-four model dependencies.
#
# The default `pip install -r requirements.txt` pulls the standard PyTorch
# wheels from PyPI: CUDA-enabled on Linux, MPS-capable on macOS, CPU fallback
# elsewhere. Training auto-detects the best device (cuda > mps > cpu).
#
# On a CPU-only machine you can skip the multi-GB CUDA wheels by installing
# torch from the CPU index first:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0
tqdm>=4.66
